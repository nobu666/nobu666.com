box: debian
build:
    steps:
        - arjen/hugo-build:
            version: "0.14"
            theme: vienna
            config: config.yaml
            flags: --buildDrafts=false
deploy:
    steps:
        - apt-get install git-core
        - lukevivier/gh-pages:
            token: $GIT_TOKEN
            domain: nobu666.com
            basedir: public
