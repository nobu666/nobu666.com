box: ruudk/ubuntu14.04@1.0.0
build:
    steps:
        - arjen/hugo-build:
            version: "0.14"
            theme: vienna
            config: config.yaml
            flags: --buildDrafts=false
deploy:
    steps:
        - lukevivier/gh-pages:
            token: $GIT_TOKEN
            domain: nobu666.com
            basedir: public
