machine:
  timezone: Asia/Tokyo

checkout:
  post:
    - git submodule update --init --recursive

dependencies:
  pre:
    - go get -v github.com/spf13/hugo
    - git config --global user.name "nobu666"
    - git config --global user.email "ogata.nobu@gmail.com"

deployment:
  master:
    branch: master
    commands:
      - git clone git@github.com:nobu666/nobu666.github.io.git publish
      - hugo
      - cd public && git add .
      - cd public && git commit -m "[ci skip] publish"
      - cd public && git push origin master
